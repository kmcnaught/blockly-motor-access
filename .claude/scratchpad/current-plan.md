# Plan


1. Make 'Houston we have a..' sound effect (when astronaut flies off path) 75% the current volume
2. add a setting to turn off sound fx - a mute button in the header. in grid mode and normal mode
3. slight delay before popping up the 'not quite..' dialog
4. make the block fields not editabele in the toolbox (flyout) because its easy to accidentally click the field instead of the block. if it helps not accidentally click in wrong place
