# Switch-Scanning Mode — Phase 1 (reset 2026-06-03 for new plan)
Step 1: done — SwitchScanController scaffold + URL-param wiring + mutual exclusion with grid coding. Review fixed 3 issues (enabled-guard, input-field skip, Space alias normalization).
Step 2: done — DOM tagging in index.html. data-scan-region on <header> and .game-container; data-scan-item on 10 buttons (5 header + 5 maze-actions). #infoBtn deferred (lives in instructionBar). Review LGTM, no fixes.
Step 3: done — top-level scan cycle visible. Region discovery (header / toolbox / workspace / maze-actions), sentinel chip, amber fixed overlay outline, rAF-throttled resize/scroll reflow, css class .switch-scan-highlight / .switch-scan-sentinel. Review fix: removed transient toolbox 0x0 drop so it self-heals via getRect thunk.
Step 4: done — header sub-scan. Refactored to discriminated-union frame stack ({kind:'top'|'dom-items'}); selecting header pushes a dom-items frame, item select fires .click() and pops, sentinel pops without click. popToTopIndex resumes top scan at header+1 (toolbox). Other regions still log-only. Review APPROVE, no fixes.
Step 5: done — maze-actions sub-scan reuses the Step-4 mechanism. Generalized to discoverDomRegionItems(regionName) / enterDomRegionSubScan(regionName, topLevelIndex). Visibility filter (offsetParent + rect width) hides .hidden ghost-run but keeps :disabled prev/next. popToTopIndex lands on top sentinel. Review APPROVE, no fixes.
Step 6: done — toolbox sub-scan + extracted shared block-insertion helper. New block-insertion.ts exports insertBlockAfterCursor (FocusManager cursor → chain-tail fallback → empty-workspace anchor). grid-coding-mode delegates to it; history bookkeeping stays at the call sites. New 'blocks' ScanFrame variant supports toolbox now and is shaped to also serve workspace in step 7. Review APPROVE, no fixes.
